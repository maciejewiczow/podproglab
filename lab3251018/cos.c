#include <math.h>
#include <stdlib.h>
#include <stdio.h>

double x, y, dx = 0.2, x1 = 5, x2 = 7, a = 3, b = 8;

FILE *f;

double log_n(double n, double x)
{
    return log(x) / log(n);
}

int main(void)
{
}